{
  "name": "@nextgen/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@nextgen/agents": "0.1.0",
    "@nextgen/rag": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "wrangler": "^4.24.0"
  }
}
